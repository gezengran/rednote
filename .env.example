# Copy to .env and set your own key (openssl rand -hex 32).
# Canvas server and MCP must use the same EXCALIDRAW_API_KEY.
EXCALIDRAW_API_KEY=replace-with-openssl-rand-hex-32
CANVAS_SERVER_URL=http://127.0.0.1:3000
